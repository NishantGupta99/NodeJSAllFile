const details = require("./second"); // used to import the object present in the second.js file and stored inside the const details
console.log("Hello world", details); // use node keyword in the terminal followed by the file name to get the output that goe sto show that node is a runtime enviroment 

