const os = require('os');
console.log(os.freemem()); // tells you the free memory in your computer    
console.log(os.homedir()); // tells you your home directory 
console.log(os.hostname()) ; // tells you the host name of your computer 
